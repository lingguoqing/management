<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.management.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.management.model.entity.SysRole">
        <id property="id" column="id" />
        <result property="roleName" column="role_name" />
        <result property="roleCode" column="role_code" />
        <result property="roleDesc" column="role_desc" />
        <result property="dataScope" column="data_scope" />
        <result property="sortOrder" column="sort_order" />
        <result property="status" column="status" />
        <result property="deleted" column="deleted" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,role_name,role_code,role_desc,data_scope,sort_order,
        status,deleted,create_time,update_time
    </sql>

    <!-- 查询用户的所有角色 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1 AND r.deleted = 0
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultMap="BaseResultMap">
        SELECT * FROM sys_role WHERE role_code = #{roleCode} AND deleted = 0 LIMIT 1
    </select>

</mapper>